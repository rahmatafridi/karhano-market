@model IEnumerable<BAL.Model.SubCategoryVM>
@{
    ViewData["Title"] = "List";
}
<section class="section">
    <div class="row" id="table-head">
        <div class="col-12">
            <div class="card">
                <div class="card-header">
                    <h4 class="card-title">SubCategory  List</h4>
                </div>

                <div>
                    <a asp-action="Add" asp-controller="SubCategory" class="btn btn-primary me-1 mb-1">Add SubCategory</a>
                </div>
                <div class="card-content">

                    <!-- table head dark -->
                    <div class="table-responsive">
                        <table class="table mb-0">
                            <thead class="thead-dark">
                                <tr>
                                    <th>Name</th>
                                        <th>Category</th>

                                    <th>Status</th>
                                    <th></th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var x in Model)
                                {
                                    <tr>
                                        <td>@x.Name</td>
                                            <td>@x.Category</td>

                                        <td>@(x.Status ? "Active" : "Inactive")</td>
                                        <td>
                                            <a asp-controller="SubCategory" asp-action="Edit" asp-route-id="@x.Id"><i class="align-middle" data-feather="edit-2"></i> Edit</a> |
                                                <a asp-action="Delete" asp-controller="SubCategory" asp-route-id="@x.Id" onclick="return confirm('Are you sure you want to delete this role?');"><i class="align-middle" data-feather="trash"></i> Delete</a>
                                        </td>
                                    </tr>
                                }

                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

