@model IEnumerable<BAL.Model.StoreTypeModel>
@{
    ViewData["Title"] = "List";
}
<section class="section">
    <div class="row" id="table-head">
        <div class="col-12">
            <div class="card">
                <div class="card-header">
                    <h4 class="card-title">Store Type  List</h4>
                </div>
                <div>
                    <a asp-action="Add" class="btn btn-primary me-1 mb-1"> Add Type</a>
                </div>
                <div class="card-content">

                    <!-- table head dark -->
                    <div class="table-responsive">
                        <table class="table mb-0">
                            <thead class="thead-dark">
                                <tr>
                                    <th>Name</th>
                                    <th>Status</th>
                                    <th></th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var user in Model)
                                {
                                    <tr>
                                        <td>@user.Name</td>
                                        <td>@(user.Status ? "Active" : "Inactive")</td>
                                        <td>
                                            <a asp-controller="StoreType" asp-action="Edit" asp-route-id="@user.Id"><i class="align-middle" data-feather="edit-2"></i> Edit</a> |
                                            <a asp-action="Delete" asp-route-id="@user.Id" onclick="return confirm('Are you sure you want to delete this role?');"><i class="align-middle" data-feather="trash"></i> Delete</a>
                                        </td>
                                    </tr>
                                }

                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>




